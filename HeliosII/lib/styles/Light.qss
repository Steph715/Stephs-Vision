/* =============================================================================
   Helios Light Theme (Modernized)
   A modern, flat, light theme with blue accents.
   ============================================================================= */

/* --- Palette ---
   Background: #e8eaed
   Panel:      #f4f5f7
   Accent:     #0066cc
   Text:       #1a1a1a
   Border:     #c4c8ce
   Inputs:     #ffffff
   Hover:      #dcdfe3
   ---------------- */

/* =============================================================================
   Global
   ============================================================================= */
QWidget {
    background-color: #e8eaed;
    color: #1a1a1a;
    font-family: "Segoe UI", "Roboto", "Helvetica Neue", sans-serif;
    font-size: 9pt;
}

QWidget:disabled {
    color: #9ca3af;
}

QWidget:focus {
    outline: none;
}

/* =============================================================================
   Main Window
   ============================================================================= */
QMainWindow {
    background-color: #e8eaed;
}

QMainWindow::separator {
    background: #c4c8ce; /* Visible separator color */
    width: 3px;
    height: 3px;
}

QMainWindow::separator:hover {
    background: #0066cc;
}

QSplitter::handle {
    background-color: #e8eaed;
}

QSplitter::handle:hover {
    background-color: #0066cc;
}

/* =============================================================================
   Dock Widgets
   ============================================================================= */
QDockWidget {
    background-color: #f4f5f7;
    border: 1px solid #c4c8ce;
    titlebar-close-icon: url(:/icons/close.svg);
    titlebar-normal-icon: url(:/icons/float.svg);
}

QDockWidget::title {
    background-color: #f4f5f7;
    color: #1a1a1a;
    padding: 5px 8px;
    font-weight: 600;
    border-bottom: 1px solid #dcdfe3;
}

QDockWidget::close-button, QDockWidget::float-button {
    background-color: transparent;
    border-radius: 4px;
    padding: 2px;
}

QDockWidget::close-button:hover, QDockWidget::float-button:hover {
    background-color: #c4c8ce;
}

/* =============================================================================
   Menu & Toolbar
   ============================================================================= */
QMenuBar {
    background-color: #f4f5f7;
    padding: 2px;
    border-bottom: 1px solid #c4c8ce;
}

QMenuBar::item {
    padding: 4px 8px;
    background: transparent;
    border-radius: 4px;
}

QMenuBar::item:selected {
    background-color: #dcdfe3;
    color: #1a1a1a;
}

QMenu {
    background-color: #f4f5f7;
    border: 1px solid #c4c8ce;
    border-radius: 6px;
    padding: 4px;
}

QMenu::item {
    padding: 6px 24px; /* Increased padding for dropdown items */
    border-radius: 4px;
}

QMenu::item:selected {
    background-color: #dcdfe3;
    color: #1a1a1a;
}

QMenu::item:pressed {
    background-color: #0066cc;
    color: white;
}

QMenu::separator {
    height: 1px;
    background-color: #c4c8ce;
    margin: 3px 8px;
}

QToolBar {
    background-color: #f4f5f7;
    border: none;
    border-bottom: 1px solid #c4c8ce;
    padding: 3px;
    spacing: 3px;
}

QToolButton {
    background-color: transparent;
    border-radius: 4px;
    padding: 6px 6px; /* Increased padding */
}

QToolButton:hover {
    background-color: #dcdfe3;
}

QToolButton:pressed {
    background-color: #0066cc;
    color: #ffffff;
}

QToolButton:checked {
    background-color: #0066cc;
    border: 1px solid #0066cc;
    color: #ffffff;
}

/* =============================================================================
   Buttons
   ============================================================================= */
QPushButton {
    background-color: #f4f5f7;
    color: #1a1a1a;
    border: 1px solid #b8bcc2;
    border-radius: 4px;
    padding: 4px 12px;
    min-height: 20px;
}

QPushButton:hover {
    background-color: #dcdfe3;
    border-color: #9ca3af;
}

QPushButton:pressed {
    background-color: #0066cc;
    color: #ffffff;
}

QPushButton:checked {
    background-color: #0066cc;
    color: white;
    border-color: #0066cc;
}

QPushButton:checked:hover {
    background-color: #0052a3;
    border-color: #0052a3;
}

QPushButton:default, QPushButton[class="primary"] {
    background-color: #0066cc;
    color: #ffffff;
}

QPushButton:default:hover {
    background-color: #0052a3;
}

/* =============================================================================
   Inputs
   ============================================================================= */
QLineEdit, QSpinBox, QDoubleSpinBox, QComboBox, QTextEdit, QPlainTextEdit {
    background-color: #ffffff;
    border: 1px solid #b8bcc2;
    border-radius: 4px;
    padding: 3px 6px;
    selection-background-color: #0066cc;
    color: #1a1a1a;
    min-height: 18px;
}

/* Output Log Text Field */
QTextEdit[objectName="outputLog"] {
    background-color: #ffffff;
    color: #1a1a1a;
}

QLineEdit:focus, QSpinBox:focus, QDoubleSpinBox:focus, QComboBox:focus, QTextEdit:focus, QPlainTextEdit:focus {
    border: 1px solid #0066cc;
    background-color: #ffffff;
}

QSpinBox::up-button, QDoubleSpinBox::up-button,
QSpinBox::down-button, QDoubleSpinBox::down-button {
    background-color: transparent;
    border: none;
    width: 16px;
}

QSpinBox::up-button:hover, QDoubleSpinBox::up-button:hover,
QSpinBox::down-button:hover, QDoubleSpinBox::down-button:hover {
    background-color: #dcdfe3;
}

QSpinBox::up-arrow, QDoubleSpinBox::up-arrow {
    image: url(:/icons/spinbox-up-arrow.svg);
    width: 10px;
    height: 10px;
}

QSpinBox::down-arrow, QDoubleSpinBox::down-arrow {
    image: url(:/icons/spinbox-down-arrow.svg);
    width: 10px;
    height: 10px;
}

/* ComboBox */
QComboBox {
    min-width: 60px;
    padding: 4px 7px;
}

QComboBox:hover {
    background-color: #dcdfe3;
    border-color: #9ca3af;
}

QComboBox::drop-down {
    border: none;
    width: 20px;
    background-color: transparent;
}

QComboBox::down-arrow {
    image: url(:/icons/arrow-down.svg);
    width: 10px;
    height: 10px;
}

QComboBox QAbstractItemView {
    background-color: #f4f5f7;
    border: 1px solid #b8bcc2;
    border-radius: 4px;
    selection-background-color: #0066cc;
    outline: none;
    padding: 4px;
}

QComboBox QAbstractItemView::item {
    padding: 4px 8px;
    border-radius: 3px;
}

QComboBox QAbstractItemView::item:hover {
    background-color: #dcdfe3;
    color: #1a1a1a;
}

/* =============================================================================
   Lists/Trees
   ============================================================================= */
QListView, QTreeView, QTableView {
    background-color: #ffffff;
    border: 1px solid #b8bcc2;
    border-radius: 4px;
    outline: none;
    alternate-background-color: #f4f5f7;
}

QListView::item, QTreeView::item {
    padding: 4px 5px; /* Increased top/bottom padding */
    border: none;
    border-radius: 3px;
}

QListView::item:hover, QTreeView::item:hover {
    background-color: #dcdfe3;
}

QListView::item:selected, QTreeView::item:selected {
    background-color: #c4d4e8;
    color: #1a1a1a;
    border-left: 2px solid #0066cc;
}

QTreeView::branch:has-children:!has-siblings:closed,
QTreeView::branch:closed:has-children:has-siblings {
    image: url(:/icons/tree-expand.svg);
}

QTreeView::branch:open:has-children:!has-siblings,
QTreeView::branch:open:has-children:has-siblings {
    image: url(:/icons/tree-collapse.svg);
}

QHeaderView::section {
    background-color: #e8eaed;
    color: #4b5563;
    border: none;
    border-right: 1px solid #dcdfe3;
    border-bottom: 1px solid #dcdfe3;
    padding: 4px;
    font-weight: 600;
    font-size: 8pt;
    text-transform: uppercase;
}

/* =============================================================================
   Scrollbars
   ============================================================================= */
QScrollBar:vertical {
    background: #e8eaed;
    width: 10px;
    margin: 0px;
}

QScrollBar::handle:vertical {
    background: #b8bcc2;
    min-height: 20px;
    border-radius: 5px;
    margin: 2px;
}

QScrollBar::handle:vertical:hover {
    background: #9ca3af;
}

QScrollBar::horizontal {
    background: #e8eaed;
    height: 10px;
    margin: 0px;
}

QScrollBar::handle:horizontal {
    background: #b8bcc2;
    min-width: 20px;
    border-radius: 5px;
    margin: 2px;
}

QScrollBar::handle:horizontal:hover {
    background: #9ca3af;
}

QScrollBar::add-line, QScrollBar::sub-line {
    width: 0px;
    height: 0px;
}

/* =============================================================================
   Tab Widgets
   ============================================================================= */
QTabWidget::pane {
    border: 1px solid #c4c8ce;
    border-top: none;
    border-radius: 0px 0px 4px 4px;
    background-color: #e8eaed;
}

QTabWidget::tab-bar {
    alignment: left;
}

QTabBar::tab {
    background-color: transparent;
    color: #6b7280;
    border: none;
    padding: 7px 13px; /* Increased padding */
    margin-right: 2px;
    border-bottom: 2px solid transparent;
}

QTabBar::tab:hover {
    color: #1a1a1a;
    background-color: #dcdfe3;
}

QTabBar::tab:selected {
    color: #0066cc;
    border-bottom: 2px solid #0066cc;
    background-color: #e8eaed;
}

/* =============================================================================
   Misc
   ============================================================================= */
QGroupBox {
    border: 1px solid #b8bcc2; /* Increased contrast */
    border-radius: 5px;
    margin-top: 22px; /* Updated to 22px */
    padding: 4px;     /* Updated to 4px */
    font-weight: bold;
}

QGroupBox::title {
    subcontrol-origin: margin;
    subcontrol-position: top left;
    padding: 0 5px;       /* Updated to 0 5px */
    margin-bottom: -20px; /* Updated margin-bottom */
    background-color: #e8eaed;
    color: #4b5563; /* Dark gray instead of accent */
}

QCheckBox { spacing: 6px; }
QCheckBox::indicator {
    width: 14px; height: 14px;
    background-color: #ffffff;
    border: 1px solid #b8bcc2;
    border-radius: 3px;
}
QCheckBox::indicator:checked {
    background-color: #0066cc;
    border-color: #0066cc;
    image: url(:/icons/check.svg);
}

QRadioButton { spacing: 6px; }
QRadioButton::indicator {
    width: 14px; height: 14px;
    background-color: #ffffff;
    border: 1px solid #b8bcc2;
    border-radius: 7px;
}
QRadioButton::indicator:checked {
    background-color: #0066cc;
    border-color: #0066cc;
    image: url(:/icons/radio-dot.svg);
}

/* Status Labels */
QLabel[status="active"] { color: #0066cc; font-weight: bold; }
QLabel[status="error"] { color: #dc2626; font-weight: bold; }
QLabel[status="info"] { color: #6b7280; font-size: 9px; }

QStatusBar {
    background-color: #f4f5f7;
    color: #4b5563;
}
